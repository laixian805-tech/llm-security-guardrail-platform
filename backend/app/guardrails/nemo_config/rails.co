define bot refuse to respond
  "I cannot comply with that request."

define flow llmsec deterministic input check
  $allowed = execute llmsec_check_input

  if not $allowed
    bot refuse to respond
    stop

define flow llmsec deterministic output check
  $allowed = execute llmsec_check_output

  if not $allowed
    bot refuse to respond
    stop
